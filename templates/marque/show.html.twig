{% extends 'base.html.twig' %}

{% block title %}Marque{% endblock %}

{% block body %}
    <h1>Marque</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ marque.id }}</td>
            </tr>
            <tr>
                <th>Libellefr</th>
                <td>{{ marque.libellefr }}</td>
            </tr>
            <tr>
                <th>Libelleen</th>
                <td>{{ marque.libelleen }}</td>
            </tr>
            <tr>
                <th>Identrepprise</th>
                <td>{{ marque.identrepprise }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ marque.createdAt ? marque.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ marque.updatedAt ? marque.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ marque.deletedAt ? marque.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CreatedBy</th>
                <td>{{ marque.createdBy }}</td>
            </tr>
            <tr>
                <th>UpdatedBy</th>
                <td>{{ marque.updatedBy }}</td>
            </tr>
            <tr>
                <th>DeletedBy</th>
                <td>{{ marque.deletedBy }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_marque_index') }}">back to list</a>

    <a href="{{ path('app_marque_edit', {'id': marque.id}) }}">edit</a>

    {{ include('marque/_delete_form.html.twig') }}
{% endblock %}
