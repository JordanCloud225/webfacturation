{% extends 'base.html.twig' %}

{% block title %}Objetdepense{% endblock %}

{% block body %}
    <h1>Objetdepense</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ objetdepense.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ objetdepense.libelle }}</td>
            </tr>
            <tr>
                <th>Identreprise</th>
                <td>{{ objetdepense.identreprise }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ objetdepense.createdAt ? objetdepense.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ objetdepense.updatedAt ? objetdepense.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ objetdepense.deletedAt ? objetdepense.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CreatedBy</th>
                <td>{{ objetdepense.createdBy }}</td>
            </tr>
            <tr>
                <th>UpdatedBy</th>
                <td>{{ objetdepense.updatedBy }}</td>
            </tr>
            <tr>
                <th>DeletedBy</th>
                <td>{{ objetdepense.deletedBy }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_objetdepense_index') }}">back to list</a>

    <a href="{{ path('app_objetdepense_edit', {'id': objetdepense.id}) }}">edit</a>

    {{ include('objetdepense/_delete_form.html.twig') }}
{% endblock %}
