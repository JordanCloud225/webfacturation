<form method="post" action="{{ path('app_boncommandefour_delete', {'id': boncommandefour.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ boncommandefour.id) }}">
    <button class="btn">Delete</button>
</form>
