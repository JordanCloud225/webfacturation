{% extends 'base.html.twig' %}

{% block title %}Boncommandeclient{% endblock %}

{% block body %}
    <h1>Boncommandeclient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ boncommandeclient.id }}</td>
            </tr>
            <tr>
                <th>Datebdccli</th>
                <td>{{ boncommandeclient.datebdccli ? boncommandeclient.datebdccli|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Numdevis</th>
                <td>{{ boncommandeclient.numdevis }}</td>
            </tr>
            <tr>
                <th>Brochure</th>
                <td>{{ boncommandeclient.brochure }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_boncommandeclient_index') }}">back to list</a>

    <a href="{{ path('app_boncommandeclient_edit', {'id': boncommandeclient.id}) }}">edit</a>

    {{ include('boncommandeclient/_delete_form.html.twig') }}
{% endblock %}
