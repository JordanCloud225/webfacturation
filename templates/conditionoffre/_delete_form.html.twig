<form method="post" action="{{ path('app_conditionoffre_delete', {'id': conditionoffre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ conditionoffre.id) }}">
    <button class="btn">Delete</button>
</form>
